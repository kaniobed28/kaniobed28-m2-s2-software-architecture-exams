meta {
  name: POST /users
  type: http
  seq: 1
}

post {
  url: http://localhost:3000/users
  body: json
  auth: inherit
}

body:json {
  {
    "username": "reader",
    "role": "reader",
    "password": "reader"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
